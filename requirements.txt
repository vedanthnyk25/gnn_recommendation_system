# requirements.txt (Final Version for CPU Deployment)

# --- Base Libraries (from PyPI) ---
streamlit
pandas
numpy
scikit-learn
requests
tqdm

# --- PyTorch Libraries (CPU VERSIONS) ---
# This section ensures torch is installed first

# Use the PyTorch CPU index as an *additional* source.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.5.1

# --- PyTorch Geometric Libraries ---
# These will be installed after torch is present.

# Find PyG links in its specific repository for the CPU TORCH VERSION
--find-links https://data.pyg.org/whl/torch-2.5.1+cpu.html
torch_geometric==2.5.0
torch_sparse
torch_scatter
torch_cluster
